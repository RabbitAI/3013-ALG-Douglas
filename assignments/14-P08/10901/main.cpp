#include <iostream>
#include <string>

using namespace std;

struct Node
{
    int time, m;
    string side;
    Node* next;
    
};

int main()
{
    int  c, n, t, m, time = 0;
    string side;
    bool left = true, right = false;

    cin >> c >> n >> t >> m;

    for(int i = 0; i < c; i++)
    {
        for(int j = 0; j < n; j++)
        {
            
        }
    }

    return 0;
}